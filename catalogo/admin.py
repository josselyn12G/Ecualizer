"""Registro de modelos en el admin de Django (vacío — managed=False)."""
# Los modelos usan managed=False (las tablas viven en SQL Server),
# por eso no los registramos en el admin de Django para evitar migraciones.
